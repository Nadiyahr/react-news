import { useEffect } from 'react';
import { sections } from '../../../assest/vars';

const TopFooter = () => {
  useEffect(() => {
    // console.log(Object.keys(sections));
  });
  return (
    <div>
      <div>
        {/* {footerSections.map((new, i) => (
        <div key={i}></div>
      ))} */}
      </div>
    </div>
  );
};

export default TopFooter;
